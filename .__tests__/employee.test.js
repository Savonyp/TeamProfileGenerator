// Import the Employee class from the '../lib/Employee' module
const Employee = require("../lib/Employee");

test("can set name", () => {
    if
});

test("can set id", () => {
    
});

test("can set email", () => {
    
});

test("can set role ", () => {

});